# Copy this file to .env and fill in real values. NEVER commit .env.
# ── Scraper ──────────────────────────────────────────────────────────
APIFY_API_TOKEN=            # apify.com console -> Settings -> API tokens

# ── Coming in later milestones (placeholders so nothing surprises us) ─
# DATABASE_URL=postgresql+psycopg://bob:bob@localhost:5432/bob   # M0.2
SECRET_KEY=                 # REQUIRED — signs login tokens. Generate with:
                            #   python -c "import secrets; print(secrets.token_urlsafe(48))"
# REDIS_URL=redis://localhost:6379/0                             # later
OPENAI_API_KEY=             # platform.openai.com — vision draft agent (gpt-4o-mini)
# GEMINI_API_KEY=           # aistudio.google.com — optional legacy vision provider
# ANTHROPIC_API_KEY=        # console.anthropic.com — reserved for the M5 customer chat agent
# DARAJA_CONSUMER_KEY=                                           # M3
# DARAJA_CONSUMER_SECRET=                                        # M3
# DARAJA_SHORTCODE=                                              # M3
# DARAJA_PASSKEY=                                                # M3
# AFRICASTALKING_API_KEY=                                        # M4
