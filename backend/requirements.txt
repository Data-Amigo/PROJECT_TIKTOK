# Backend dependencies — pinned exact versions so every machine (yours, a
# tester's, the VPS) runs the same code. Update deliberately, never by accident.
# Install:  .venv/Scripts/python -m pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────
fastapi==0.139.2
uvicorn[standard]==0.51.0        # the ASGI server that runs the app

# ── Config & validation ───────────────────────────────────
pydantic==2.13.4
pydantic-settings==2.14.2        # typed .env loading (app/config.py)
python-dotenv==1.2.2             # spike scripts read .env directly

# ── Database ──────────────────────────────────────────────
sqlalchemy==2.0.51
psycopg[binary]==3.3.4           # the actual Postgres driver (binary = no compiler needed)
alembic==1.18.5                  # schema migrations (backend/alembic/)

# ── AI (vision draft agent) ───────────────────────────────
openai==2.48.0                   # GPT vision + structured outputs (product drafts)
# google-genai kept installed as an optional legacy vision provider

# ── Auth ──────────────────────────────────────────────────
argon2-cffi==25.1.0              # Argon2id password hashing (OWASP-recommended)
PyJWT==2.13.0                    # signed JWT access tokens
email-validator==2.3.0           # backs pydantic EmailStr

# ── HTTP clients ──────────────────────────────────────────
requests==2.34.2                 # sync client (spikes); services move to httpx
httpx==0.28.1                    # async-capable client + powers TestClient

# ── Testing ───────────────────────────────────────────────
pytest==9.1.1
